{
  "buildCommand": "CI=false npm run build",
  "outputDirectory": "build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "REACT_APP_ENV": "production",
    "REACT_APP_MENU_URL": "https://menu.comensaia.com"
  }
}
